---
description: Design, validate, and write a new OpenCode agent (interactive).
agent: opencode-agent-expert
subtask: true
---

The user wants a new OpenCode agent. Treat the contents of $ARGUMENTS as the initial description and purpose. Load the `opencode-agent-expert` skill for the schema, then ask the missing questions, then produce a strict-mode-validated agent file at the location the user chooses.
