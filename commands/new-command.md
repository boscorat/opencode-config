---
description: Design, validate, and write a new OpenCode command (interactive).
agent: opencode-agent-expert
subtask: true
---

The user wants a new OpenCode command. Treat the contents of $ARGUMENTS as the initial description and purpose. Load the `opencode-agent-expert` skill for the schema, then ask the missing questions, then produce a strict-mode-validated command file at the location the user chooses.
