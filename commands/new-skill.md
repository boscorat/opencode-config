---
description: Design, validate, and write a new OpenCode skill (interactive).
agent: opencode-agent-expert
subtask: true
---

The user wants a new OpenCode skill. Treat the contents of $ARGUMENTS as the initial description and purpose. Load the `opencode-agent-expert` skill for the schema, then ask the missing questions, then produce a strict-mode-validated `SKILL.md` (with the correct directory layout) at the location the user chooses.
